let framerate = null
let delta = 0;
let test=0
let fontSize=24
nalert("goof")
update = (delta) => {
    clear()
    button("Click", W/2-25,H/2-25,function(){}, null,null, "white", Math.abs(fontSize), 10)
}
